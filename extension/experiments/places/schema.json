[
  {
    "namespace": "experiments.places",
    "description": "Privileged access to the Places database (places.sqlite).",
    "types": [
      {
        "id": "TableRef",
        "type": "object",
        "description": "A table in one of the schemas attached to the Places connection.",
        "properties": {
          "schema": {
            "type": "string",
            "description": "The attached schema name, e.g. 'main' or 'favicons'."
          },
          "name": {
            "type": "string",
            "description": "The table name, e.g. 'moz_places'."
          },
          "label": {
            "type": "string",
            "description": "Display label: 'moz_places' for main, 'favicons.moz_icons' otherwise."
          }
        }
      }
    ],
    "functions": [
      {
        "name": "getTables",
        "type": "function",
        "description": "List the tables and views across every schema attached to the Places connection.",
        "async": true,
        "parameters": []
      },
      {
        "name": "getRows",
        "type": "function",
        "description": "Return rows from a table, along with its column names. Some tables gain extra derived columns (reported in derivedColumns) that resolve foreign keys to something readable.",
        "async": true,
        "parameters": [
          {
            "type": "object",
            "name": "options",
            "properties": {
              "schema": {
                "type": "string",
                "description": "The attached schema the table belongs to."
              },
              "table": {
                "type": "string",
                "description": "The table to read."
              },
              "limit": {
                "type": "integer",
                "optional": true,
                "minimum": 0,
                "description": "Maximum number of rows to return. 0 means no limit."
              },
              "orderBy": {
                "type": "string",
                "optional": true,
                "description": "Column to sort by. Must be a real column of the table."
              },
              "descending": {
                "type": "boolean",
                "optional": true,
                "description": "Sort in descending order."
              },
              "filter": {
                "type": "string",
                "optional": true,
                "description": "Case-insensitive substring matched against every text column."
              }
            }
          }
        ]
      },
      {
        "name": "getFrecencyBreakdown",
        "type": "function",
        "description": "Re-run the calculate_frecency query for one page, returning the sampled visits, their weights and decay, and the terms of the final formula.",
        "async": true,
        "parameters": [
          {
            "type": "object",
            "name": "options",
            "properties": {
              "pageId": {
                "type": "integer",
                "description": "The moz_places id to explain."
              },
              "isRedirect": {
                "type": "boolean",
                "optional": true,
                "description": "Whether to compute as if the page were a redirect target, matching calculate_frecency's second argument."
              }
            }
          }
        ]
      },
      {
        "name": "benchmarkFrecency",
        "type": "function",
        "description": "Time the real calculate_frecency function for a sample of pages, on the main thread against Places' main connection. Returns per-page timings alongside each page's visit count.",
        "async": true,
        "parameters": [
          {
            "type": "object",
            "name": "options",
            "properties": {
              "sampleSize": {
                "type": "integer",
                "optional": true,
                "minimum": 1,
                "maximum": 10000,
                "description": "Number of random pages to time. The busiest tenth as many again are added on top."
              },
              "iterations": {
                "type": "integer",
                "optional": true,
                "minimum": 1,
                "maximum": 100,
                "description": "Timed calls per page, after one warm-up call. The median is reported."
              }
            }
          }
        ]
      }
    ]
  }
]
